<!DOCTYPE html>
<html lang="en">
  <%- include("./partials/head.ejs")%>
  <body>
    <%- include("./partials/body.ejs") %>

    <% places.forEach((place) => { %>
    <a href="/places/<%= place.id %>">
      <p><%= place.name %></p>
    </a>
    <% }) %>

  </body>
</html>
