<!DOCTYPE html>
<html lang="en">
  <%- include("./partials/head.ejs") %>
  <body>
    <%- include("./partials/body.ejs") %>

    <div class="places-container" id="hide">
      <% places.forEach((place) => { %>
      <a href="/places/<%= place.id %>">
        <p class="list"><%= place.name %></p>
      </a>
      <% }) %>
    </div>

  </body>
</html>
